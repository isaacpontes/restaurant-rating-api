const path = require("path");

module.exports = {
    config: path.join('config', 'sequelize-cli.js'),
    'models-path': path.join('src', 'models'),
    'migrations-path': path.join('src', 'database', 'migrations'),
    'seeders-path': path.join('src', 'database', 'seeders')
}